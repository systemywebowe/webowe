<%= form_for(@partner_project) do |f| %>
  <% if @partner_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partner_project.errors.count, "error") %> prohibited this partner_project from being saved:</h2>

      <ul>
      <% @partner_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_partnerProject %><br />
    <%= f.number_field :id_partnerProject %>
  </div>
  <div class="field">
    <%= f.label :id_partner %><br />
    <%= f.number_field :id_partner %>
  </div>
  <div class="field">
    <%= f.label :id_project %><br />
    <%= f.number_field :id_project %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
