<%= form_for(@sponsor_project) do |f| %>
  <% if @sponsor_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sponsor_project.errors.count, "error") %> prohibited this sponsor_project from being saved:</h2>

      <ul>
      <% @sponsor_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_sponsorProject %><br />
    <%= f.number_field :id_sponsorProject %>
  </div>
  <div class="field">
    <%= f.label :id_sponsor %><br />
    <%= f.number_field :id_sponsor %>
  </div>
  <div class="field">
    <%= f.label :id_project %><br />
    <%= f.number_field :id_project %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
