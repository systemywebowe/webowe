<!DOCTYPE html>
<html>
<head>
  <title>Webowe</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<center>
	<table style="text-align:center"><tr style="background-color: white"><td colspan="2" style="text-align: right">
		<%= link_to image_tag('/images/banner.jpg'), '/home' %><br>
		<% if signed_in? %>
		Logged as: <b><%= current_user.login %></b>
        <%= link_to "Profile", user_path(current_user) %> | 
        <%= link_to "Logout", '/signout' %>
        <% else %>
        <%= link_to "Register", new_user_path%> | <%= link_to "Login", signin_path %>
        <% end %>
		<% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
     </td></tr>
     <tr><td style="vertical-align:top">
     	MENU
<div id="SlideMenu1">
</div>
     </td><td width="600px">
<%= yield %>
</td></tr>
<tr style="background-color: white"><td colspan="2">
About | Privacy Policy | Contact<br>
Wrocław 2013
<!--%= debug(params) %>-->
</td></tr></table>
</center>
</body>
</html>
