<h1>Listing awards</h1>

<table>
  <tr>
    <th>Id award</th>
    <th>Id project</th>
    <th>Name</th>
    <th>Content</th>
    <th>From</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @awards.each do |award| %>
  <tr>
    <td><%= award.id_award %></td>
    <td><%= award.id_project %></td>
    <td><%= award.name %></td>
    <td><%= award.content %></td>
    <td><%= award.from %></td>
    <td><%= link_to 'Show', award %></td>
    <td><%= link_to 'Edit', edit_award_path(award) %></td>
    <td><%= link_to 'Destroy', award, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Award', new_award_path %>
