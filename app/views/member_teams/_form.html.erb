<!DOCTYPE html>
<html>
<head>
  <title>Webowe</title>
  <%= stylesheet_link_tag    "main", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<center>
<%= form_for(@member_team) do |f| %>
  <% if @member_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member_team.errors.count, "error") %> prohibited this member_team from being saved:</h2>

      <ul>
      <% @member_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<table>
		<tr><td style="text-align: right">
    <%= f.label :function %></td><td style="text-align: left"><%= f.text_field :function %>
   		</td></tr>
		<tr><td style="text-align: right">
    <%= f.label :member_id %></td><td style="text-align: left"><%= f.collection_select(:member_id, @members, :id, :surname) %>
   		</td></tr>
   		</table>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</center>
</body>
</html>
